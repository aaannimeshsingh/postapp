<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
            <a class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block" href="/logout">logout</a>
        </div>
        <div class="flex items-start gap-3">
        <div class="w-10 h-10 bg-red-500 rounded-md overflow-hidden">
            <img class="w-full h-full object-cover" src="/images/uploads/<%= user.profilepic %>" alt="">
        </div>
        <h3 class="text-3xl mb-5"><span class="font-light">Hello</span> <%= user.name %>👋🏿</h3>
        </div>
        <a href="/profile/upload">Edit profilepic</a>
        <h5 class="mb-5 mt-3">Here you can create new post</h5>
        <form action="/post" method="post">
            <textarea placeholder="What's on your mind" class="block w-1/3 outline-none p-3 resize-none bg-transparent border-2 border-zinc-800 rounded-md" name="content" id=""></textarea>
            <input class="block w-40 px-3 py-2 text-sm bg-blue-500 block rounded-md mt-2" type="submit" value="Create new post">
        </form>
        <div class="posts mt-20">
            <h3 class="text-zinc-400">Your Posts.</h3>
            <div class="postcontainer mt-5">
                <% user.posts.reverse().forEach(function(post){ %>
                    <div class="post w-1/3 p-4 mb-2 rounded-md border-[1px] border-zinc-700 bg-zinc-8000">
                    <h4 class="text-blue-500 mb-5"><%= user.name %></h4>
                    <p class="text-sm tracking-tighter"><%= post.content %></p>
                    <small class="mt-2 inline-block"><%= post.likes.length %></small>
                    <div class="btns gap-4 flex mt-1">
                        <a class="text-blue-500" href="/like/<%= post._id %>">
                            <%= post.likes.indexOf(user._id) ? "Like":"Unlike" %>
                        </a>
                        <a class="text-zinc-400" href="/edit/<%= post._id %>">edit</a>
                    </div>
                </div>
                <% }) %>
            </div>
        </div>
    </div>
</body>
</html>